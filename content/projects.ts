import { allProjects } from "./projects/index";

/**
 * Featured projects array.
 * Individual detailed project files reside inside content/projects/ for clean maintenance.
 */
export const projects = allProjects;
